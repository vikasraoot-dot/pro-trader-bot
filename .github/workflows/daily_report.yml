name: Daily Report (Self-Hosted)

on:
  workflow_dispatch:
  schedule:
    - cron: "10 20 * * 1-5"  # ~16:10 ET (adjust for DST if you prefer)

permissions:
  contents: read

concurrency:
  group: daily-report
  cancel-in-progress: true

jobs:
  report:
    runs-on: [self-hosted, windows]
    env:
      PYTHONPATH: ${{ github.workspace }}
      ALPACA_KEY: ${{ secrets.ALPACA_KEY }}
      ALPACA_SECRET: ${{ secrets.ALPACA_SECRET }}
      APCA_BASE_URL: ${{ vars.APCA_BASE_URL }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      TICKERS: ${{ vars.TICKERS }}
      CONFIG_PATH: ${{ vars.CONFIG_PATH }}

    steps:
      - uses: actions/checkout@v4

      - name: Show Python
        run: |
          whoami
          python --version
          where python

      - name: Install deps (binary wheels only)
        env:
          PIP_ONLY_BINARY: ":all:"
        run: |
          python -m pip install --upgrade pip wheel setuptools
          pip install --only-binary=:all: numpy==1.26.4 pandas==2.2.2
          pip install --only-binary=:all: -r requirements.txt

      - name: Generate daily report
        run: |
          python scripts/daily_report.py --config "${{ env.CONFIG_PATH }}"

      - name: Upload report artifacts
        uses: actions/upload-artifact@v4
        with:
          name: daily-report
          path: reports/
